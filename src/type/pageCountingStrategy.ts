export enum PageCountingStrategy {
  Normal = "Normal", // Standard counting (1, 2, 3, ...)
  OddOnly = "Odd Only", // Counting only odd pages (1, 3, 5, ...)
  EvenOnly = "Even Only", // Counting only even pages (2, 4, 6, ...)
}
