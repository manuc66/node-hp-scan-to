anguage: node_js
node_js:
  - "10"
  - "node"
env:
  - NODE_ENV=TEST
cache:
  yarn: true
  directories:
    - "node_modules"
before_script:
  - yarn global add if-node-version
script:
  - yarn build || travis_terminate 1
